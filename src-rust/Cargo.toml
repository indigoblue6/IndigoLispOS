[package]
name = "indigo_lisp_os"
version = "0.2.0"
edition = "2021"
authors = ["IndigoLispOS Team"]

[lib]
crate-type = ["staticlib"]

[profile.release]
panic = "abort"
lto = true
opt-level = "z"

[profile.dev]
panic = "abort"

[dependencies]
heapless = "0.8"
